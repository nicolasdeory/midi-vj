{
  "name": "midi-vj",
  "private": true,
  "scripts": {
    "build": "tsgo -b",
    "typecheck": "tsgo -b --noEmit",
    "dev": "pnpm build && concurrently -n core,server,web -c blue,green,magenta \"pnpm --filter @midi-vj/core dev\" \"pnpm --filter @midi-vj/server dev\" \"pnpm --filter @midi-vj/web dev\""
  },
  "devDependencies": {
    "@typescript/native-preview": "7.0.0-dev.20260519.1",
    "concurrently": "^9.2.0"
  }
}
